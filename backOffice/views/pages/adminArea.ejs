<!DOCTYPE html>
<html lang="pt">
  <head>
    <%- include('../partials/head'); %>
  </head>
  <body>
    <header>
      <!-- Navbar-->
      <%- include('../partials/nav'); %>
    </header>
      
    <%- include('../partials/alertButton'); %>

    <main>
      <!-- Podia ser um switch, mas dá erro -->
      <% if (pageType === 'admin' && userType === 'admin') { %>
        <%- include('../administrators/index'); %>
      <% } else if (pageType === 'employee' && userType === 'admin') { %>
        <%- include('../employees/index'); %>
      <% } else if(pageType === 'statistics' && userType === 'admin') { %>
        <%- include('../statistics/index'); %>
      <% } else if (pageType === 'entity') { %>
          <%- include('../entities/index'); %>
      <% } else if (pageType === 'donor') { %>
        <%- include('../donors/index'); %>
      <% } else if (pageType === 'donation') { %>
        <%- include('../donations/index'); %>
      <% } else if (pageType === 'store' && userType === 'admin') { %>
        <%- include('../stores/index'); %>
      <% } else if (pageType === 'condition' && userType === 'admin') { %>
        <%- include('../conditions/index'); %>
      <% } %>
    </main>


    <!-- scripts-->
    <script
    src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
    crossorigin="anonymous">
    </script>
    <script src="/js/updateAlerts.js"></script>

  </body>
</html>

<script>
  function confirmar() {
    let texto = "Tem a certeza que pretende eliminar este registo?";
    
    if (confirm(texto) == true) {
      return true;
    }

    return false;
  }
</script>

